import { Injectable } from "@angular/core";
var DomProvider = (function () {
    function DomProvider() {
    }
    DomProvider.prototype.checkIsParent = function (parent, child) {
        var is = false;
        while (parent.parentNode) {
            if (parent == child) {
                is = true;
                break;
            }
            else if (child) {
                child = child.parentElement;
                if (child == document.body) {
                    break;
                }
                if (child == document.documentElement) {
                    break;
                }
            }
        }
        return is;
    };
    DomProvider.prototype.checkIsScrollToEnd = function (element, direction) {
        var parent = element;
        while (parent.clientHeight == parent.scrollHeight) {
            if (parent.offsetParent) {
                parent = parent.offsetParent;
            }
            else {
                break;
            }
            if (parent == document.body || parent == document.body.parentElement || parent == document.documentElement) {
                break;
            }
        }
        if (!parent) {
            return 0;
        }
        var scrollTop = parent.scrollTop;
        var viewHeight = parent.offsetHeight;
        var height = parent.scrollHeight;
        if (scrollTop == 0 && direction == 1) {
            return 1;
        }
        else if (scrollTop + viewHeight >= height && direction == -1) {
            return -1;
        }
        else {
            return 0;
        }
    };
    return DomProvider;
}());
export { DomProvider };
DomProvider.decorators = [
    { type: Injectable },
];
DomProvider.ctorParameters = function () { return []; };
//# sourceMappingURL=dom.js.map