import { ElementRef, Renderer2 } from "@angular/core";
import { NgModel, NgControl, ControlValueAccessor } from "@angular/forms";
export declare class SelectorComponent implements ControlValueAccessor {
    private element;
    private renderer2;
    private ngModel;
    private ngControl;
    writeValue(obj: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    setDisabledState?(isDisabled: boolean): void;
    constructor(element: ElementRef, renderer2: Renderer2, ngModel: NgModel, ngControl: NgControl);
    private parentElement;
    ngOnInit(): void;
    focus(e: any): void;
    blur(e: any): void;
}
